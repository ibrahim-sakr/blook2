import { Component, ViewEncapsulation } from '@angular/core';

@Component({
    selector: 'regions-root',
    template: '<router-outlet></router-outlet>',
    encapsulation: ViewEncapsulation.None
})
export class RegionsRootComponent { }
